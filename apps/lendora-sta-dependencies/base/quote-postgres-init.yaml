apiVersion: batch/v1
kind: Job
metadata:
  name: quote-postgres-init
  labels:
    app.kubernetes.io/name: quote-postgres-init
    app.kubernetes.io/part-of: lendora
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: quote-postgres-init
        app.kubernetes.io/part-of: lendora
    spec:
      restartPolicy: OnFailure
      containers:
        - name: quote-postgres-init
          image: postgres:16.6
          env:
            - name: PGHOST
              value: postgres.lendora-sta-postgres.svc.cluster.local
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: postgres-auth
                  key: POSTGRES_USER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-auth
                  key: POSTGRES_PASSWORD
            - name: QUOTE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quote-api-runtime
                  key: db-password
          command:
            - sh
            - -ceu
            - |
              psql -d postgres -v ON_ERROR_STOP=1 -v quote_password="${QUOTE_DB_PASSWORD}" <<SQL
              SELECT format('CREATE ROLE quote LOGIN PASSWORD %L', :'quote_password')
              WHERE NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'quote')\gexec
              SELECT 'CREATE DATABASE quote OWNER quote'
              WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = 'quote')\gexec
              SQL
